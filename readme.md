Hello
1. As you can see i used the methodology BEM, the reason is that I just learned how to use it.


*border radius(page)-30px
*border radius(items_inside_the_page)-20px
*space between blocks-60px
*line height in paragraphs-40px


* It took a lot to just make the text not to go under the number that i put with "BEFORE" and actually I didnt find the solution and accidently i fix it, that was a luck because I was trying different ways to make it happen.

* I used empty div classes for the lines, but at the end I had to use border, and i think next time i would use border instead of using empty div classes, that would be cleaner html code.
